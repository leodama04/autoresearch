[project]
name = "autoresearch"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "ollama",
    "requests",
    "pyarrow",
    "tiktoken",
    "torch",
    "rustbpe",
    "kernels",
]